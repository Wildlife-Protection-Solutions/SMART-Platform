To include required dependencies in the mac build add a jre and MacOS/data folder 
to an "items" directory. This will include these automatically in the build and make the
build process faster.

items/jre
items/MacOS/data